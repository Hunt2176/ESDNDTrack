<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mine.hunter.com.esdndtrack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Recycler">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SetLevel"
            android:label="@string/title_activity_set_level"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LevelSet" />
        <activity android:name=".CharacterCreater" />
        <activity android:name=".DiceRoller"></activity>
    </application>

</manifest>